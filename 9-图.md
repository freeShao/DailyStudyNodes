# 一、 图的基本概念

## 1.1. 度

无向图的度与边存在等式关系：$2E = \sum^n_{i=1} TD_{v_i}$ 

> **注意点**：根据等式关系，推得顶点总数。
> ![800](image/9-图/图的基本概念-度.png)
> 依题意，只有度为 4、3、2 的顶点，而总边数、度 4 和度 3 顶点的个数已知，带入公式即可求得度 2 的顶点数。
> $2E = 4 \cdot 5 + 3 \cdot 4 + 2 \cdot N_2$
> => $N_2 = 7$
> $N = N_4 + N_3 + N_2 = 5 + 4 + 7 = 16$


> **注意点**：有向图中如果两两顶点之间存在相互的方向，则之为 **有向完全图**，是比 **强连通图** 更强的连通情况。
> ![800](image/9-图/图的基本概念-度2.png)
> 这里顶点的度要达到最大，说明这是一个有向完全图，此时顶点之间两两相互指向。此时顶点的出度和入度均为 `n-1`，则总度数为 `2n-2`。


> **注意点**：根据无向图度和边数的等量关系，依次往后推。但这里需要考虑顶点数的至多至少的问题。
> ![800](image/9-图/图的基本概念-度3.png)
> 无向图中不存在度为 0 的顶点，因此带入等式：
> $2 \times 16 = 4 \cdot 3 + 3 \cdot 4 + 2 \cdot N_2 + 1 \cdot N_1$
> 得到等量关系：$8 = 2 \cdot N_2 + N_1$
> 当 $N_2$ 取最大时，顶点数最少；当 $N_2$ 取最小时，顶点数最多。
> $N_1 = 0; \  N_2 = 4$ => $N = 3 + 4 + 4 + 0 = 11$

---

## 1.2. 连通图

> **注意点**：概念题绘制几个草图模拟即可。
> ![800](image/9-图/图的基本概念-连通图.png)
> **无向连通图**：在任意两顶点之间存在 **通路**。(*这里不是完全图，因此不需要任意两个顶点必须直接相连*)
> 因此任意绘制草图即可。
> Ⅱ：错误在于如果存在环，那么边数和顶点数是可以相等的；
> Ⅲ：错误在于如果只是一条单链形式，那么两端的度一定是 `1`，因此有 `2` 个度为 `1` 的顶点。


> **注意点**：任何情况都需要连通，那么此时必定存在一个 `n-1` 个顶点的完全图。最少的边的情况就只需要在完全图中任意一个顶点和第 `n` 个顶点相连即可达成连通图的条件。
> ![800](image/9-图/图的基本概念-连通图2.png)
> 只需构建 6 个顶点的完全图，此时边数和顶点数的等价关系为：$1 + 2 + 3 + …… + n-1 = \frac{n(n-1)}{2}$
> 此时 6 个顶点的完全图一共有 15 条边，那么还需 1 条边与第 7 个顶点相连即可实现连通图。
> 因此边数最少需要 16 条。


> **注意点**：概念题绘制草图模拟即可。
> ![800](image/9-图/图的基本概念-连通图3.png)

---

# 二、 图的存储及基本操作

## 2.1. 时间复杂度

> **注意点**：无论是有向图还是无向图，在不同的存储结构下的时间复杂度均为 `O(n + e)`，即需要遍历所有的顶点和所有的边。
> ![800](image/9-图/图的存储及基本操作-时间空间复杂度.png)

---

## 2.2. 度

> **注意点**：取 i 行和，i 列和作为该顶点的度。代表意思是：[i 行：表示顶点 a 的出度；i 列：表示顶点 a 的入度]
> ![800](image/9-图/图的存储及基本操作-度.png)


> **注意点**：注意是无向图，在邻接多重表中连续指向的位置代表是和初始顶点相连接的顶点。
> ![800](image/9-图/图的存储及基本操作-度2.png)
> 因此此题只需要从顶点 b 和顶点 d 分别跟随指针走向，每到达一个顶点就就记作一个度。
> 其中顶点 b 连接的是：3d，0a 两个顶点；顶点 d 连接的是：2c，4e，0a，1b 四个顶点。因此其度分别为 2，4。

---

# 三、 图的遍历

## 3.1. 时间空间复杂度

> **注意点**：无论是哪种遍历，在面对邻接表存储结构的时候，时间复杂度均是 `O(n + e)`；空间复杂度均是 `O(n)`。
> 因为都需要将每个顶点和每条边均遍历一遍，且遍历过的顶点需要存储到 `list` 表中作为记录。
> ![800](image/9-图/图的遍历-时间空间复杂度.png)


> **注意点**：和上题不同在于，这里使用邻接矩阵存储结构。此时就需要注意邻接矩阵的每个顶点需要重复多遍历一遍，即时间复杂度是 `O(n^2)`。
> ![800](image/9-图/图的遍历-时间空间复杂度2.png)


> **注意点**：无须考虑是有向图还是无向图，只需要关注 **使用的是何种存储结构**。
> 若是邻接表存储，其时间复杂度是 `O(n + e)`；若是邻接矩阵存储，其时间复杂度是 `O(n^2)`
> ![800](image/9-图/图的遍历-时间空间复杂度3.png)

---

## 3.2. 遍历操作的序列

### 3.2.1. 深度优先遍历(DFS)

> **注意点**：按照边集画出该图的结构即可。**需要注意图的类型、从何处开始、使用何种遍历。**
> ![800](image/9-图/遍历操作的序列-深度优先遍历(DFS).png)
> 深度优先遍历，一直按照一条路径走到底，若未遍历完成，则回退到上一顶点查看是否有另一路径。
> A：`abe` 序列之后应该是 `d` 而不是 `c`，`e` 和 `c` 之间没有直接的连线。
> B：`acf` 序列之后应该是 `d` 而不是 `e`，`f` 和 `e` 直接没有直接的连线。
> C：`aeb` 序列之后到头了，那么接下来应该是回退到顶点 `e`，从 `e` 开始下一个顶点应该是 `d`，而不是从 `a` 开始。


> **注意点**：各项均是从顶点 a 开始遍历，因此第二个顶点有三种选择：b、e、c。
> ![800](image/9-图/遍历操作的序列-深度优先遍历(DFS)2.png)
> 2：`ac` 序列之后到头了，此时回退到顶点 `a`，下一个顶点应该是 `b/e` 中任意一个；
> 5：`ae` 序列后下一个顶点可以是 `b/d/f` 中任意一个，而 `e` 并没有和 `c` 有直接的连线，因此下一个遍历的顶点不可能是 `c`。


> **注意点**：根须题目要求画出草图。这里需要注意，边集是**从左指向右**。
> ![800](image/9-图/遍历操作的序列-深度优先遍历(DFS)3.png)
> 这里从起始顶点 V0 开始遍历即可。若一次遍历并没有完成整个序列的遍历，那么就需要回退到上一个顶点，寻找可以的路径。


> **注意点**：这个一个有向图。选项均是从顶点 V1 开始遍历的。
> ![800](image/9-图/遍历操作的序列-深度优先遍历(DFS)4.png)
> D：`V1V2` 序列之后应该是顶点 `V5`，`V2` 没有到达 `V3` 的指向。


> **注意点**：DFS 遍历时，若出现下一个顶点已经存在于 List 中，那么就说明存在回路。
> ![800](image/9-图/遍历操作的序列-深度优先遍历(DFS)5.png)

---

### 3.2.2. 广度优先遍历(BFS)

> **注意点**：只需看右侧的图 G 即可。需要额外注意的是：图 G 分为左右两个部分，顶点 5 没有到顶点 3 的指向，顶点 6 没有到顶点 3 的指向。
> ![800](image/9-图/遍历操作的序列-广度优先遍历(BFS).png)


> **注意点**：广度优先遍历需要先遍历和自己直接相邻的所有顶点，然后在这些相邻顶点中任选一个作为下一个 BFS 的起点。
> ![800](image/9-图/遍历操作的序列-广度优先遍历(BFS)2.png)
> D 项：顶点 `a` 并没有完全将和它相连的顶点遍历完成，因此**不会**先去遍历顶点 `b` 所连接的顶点 `c`。

---

# 四、 图的应用

## 4.1. 最小生成树

> **注意点**：Prim 和 Kruskal 算法是两种构造最小生成树的方式，但它们的最小生成树是可能相同的。
> ![800](image/9-图/图的应用-最小生成树.png)
> Prim 算法是 **基于顶点** 构造最小生成树，首个顶点需要找到图中和它相连的，且边权最小的顶点作为下一个连接顶点；下一次就是找图中和这两个顶点相连，且边权最小的顶点作为下一个连接顶点，若是有多个相同边权的顶点，那么就任选一个即可，照这样依次往下推进。
> Kruskal 算法是 **基于边权** 构造最小生成树，首组相连的两个顶点之间的边权必定是整个图中最小的边权；下一个同样找最小的边权，若是此时整个最小边权所连接的顶点并没有和当前已有的连通分量连接，那么就采用，否则不采用；若是有同样的边权，在满足前面的要求的情况下，任选一个即可，照这样依次往下推进。


> **注意点**：基于上题中分析的原理。Prim 中不可能凭空诞生两个没有出现过的点；Kruskal 中不可能出现已有的连通分量再次被连通。
> ![800](image/9-图/图的应用-最小生成树3.png)


> **注意点**：Prim 和 Kruskal 算法和最短路径无关。最短路径算法是 Dijkstra 算法和 Floyd 算法。
> ![800](image/9-图/图的应用-最小生成树2.png)
> 关于广度优先搜索算法也就是 BFS 广度优先遍历，是从源顶点开始，依次遍历和它直接相连的顶点，然后再依次遍历下去，这样必定可以找到源顶点和其他所有顶点之间的最短通路。


> **注意点**：Prim 算法从初始点开始寻找与其相连的最短边，保证下一个连接的顶点未连接过；Kruskal 算法从所有边中选择最短的边开始，每次需保证连接前的两个连通分量不是互连的。
> ![800](image/9-图/图的应用-最小生成树3.jpg)
> 这里说第二次选中，首先需要分别列出 K1 和 P1 记作两个算法第一次执行情况。
> K2 时，因为有三条相同的最短边，因此存在三种情况 `V1-V3; V3-V4; V2-V3`，但是 P1 只有 V1 和 V4 两个顶点，因此 `V2-V3` 这个边无法被 Prim 算法选中。
> Prim 算法的选边需要该边和已经选中的某顶点相连。

---

## 4.2. 最短路径

### 4.2.1. Dijkstra算法(单源最短)

> **注意点**：
> **最短路径**：在图中从起点到终点的路径，其边权重之和最小。最短路径可能不唯一，但所有最短路径的总权重相同；
> **简单路径**：类路径中不包含重复顶点或重复边，也就是说不存在回路。
> ![800](image/9-图/最短路径-Dijkstra算法(单源最短).jpg)
> 如果一个最短路径中还有回路，那么去掉回路，还能更短；
> 如果时存在有负权值的回路，那么每次多走回路还能更短，因此不存在最短路径。
> Dijkstra 算法适合求解**有回路的带权图**的最短路径，也可以求**任意两个顶点**的最短路径，**不适合求带负权值**的最短路径问题。


> **注意点**：Dijkstra 算法不可求带负权值图的最短路径。Floyd 算法允许每对顶点间的最短路径存在负边，但**不允许包含总权值为负的回路**。
> ![800](image/9-图/最短路径-Dijkstra算法(单源最短)2.jpg)
> 在负权图中，Dijkstra 算法既不能保证每次选出的顶点都是真正的最近顶点，又不能保证已确定的最短路径不再被改变，因此 Dijsktra 算法不允许边的权为负。
> 每对顶点间的最短路径需要调用 Dijkstra 算法 n 次，时间复杂度 `O(n^3)`。


> **注意点**：单源最短路径问题，需要注意每一轮需要确定一个新的源点，即当前权值最小的点作为新源点。
> ![800](image/9-图/最短路径-Dijkstra算法(单源最短)3.jpg)
> 第一条最短路径的目标顶点是 `b`，`a-b` 的距离是 `2`。
> 从 `b` 点出发，下一个最近的顶点是 `c`；
> 从 `C` 点出发，下一个最近的顶点是 `f`；
> 从 `f` 点出发，下一个最近的顶点是 `d`；
> 最后还剩一个顶点即 `e`。
> 因此后续目标顶点一次是 `f、d、e`。


> **注意点**：单源最短路径问题，若是新源点到某顶点之间的距离**大于**之前的源点到该点的距离，那么就不更新这个距离。
> ![800](image/9-图/最短路径-Dijkstra算法(单源最短)4.jpg)
> 第一条最短路径的目标顶点是 `5`，此时更新源点为 `5`。
> 从 `5` 点出发，更新最短路径表，发现最近的是从 `1-2`，因此目标顶点是 `2`，更新源点为 `2`；
> 从 `2` 点出发，更新最短路径表，下一个最近的顶点是 `3`；
> 从 `3` 点出发，更新最短路径表，发现最近的是从 `1-5-6`，因此目标顶点是 `6`，更新源点为 `6`。
> 从 `6` 点出发，更新最短路径表，发现最近的是从 `1-2-4`，到此从 `1` 到各顶点之间的最短路径的顶点序列已经确定：`5, 2, 3, 6, 4`。


> **注意点**：
> ![800](image/9-图/最短路径-Dijkstra算法(单源最短)5.jpg)
> 


> **注意点**：dist 数组中存储的是从源点 1 到各顶点之间的最短距离。
> **需要额外注意的是：每次更新源点之后更新 dist 内容时，需要从新源点查看到其他顶点之间的路径距离是否更短。如果更短需要更新，否则不变。**
> ![800](image/9-图/最短路径-Dijkstra算法(单源最短)6.jpg)
> 当确定第二条最短路径时，此时源点更新为 `5`，那么从 `5` 到 `2` 和 `3` 的距离分别是 `21`、`14`。
> 此时的 dist 数组中存储的是 `[21, 3, 14, 6]`。

---

### 4.2.1. Floyd算法(多源最短)
**Floyd 算法**：至今没有考过相关题目(*大题也未考过*)，但是是多源最短路径唯一一个考点。
必要的话需要记住其基本概念，以及如果需要考察应用大题，需要明白怎么构造的过程是什么。

> **注意点**：Dijkstra 算法不可求带负权值图的最短路径。Floyd 算法允许每对顶点间的最短路径存在负边，但**不允许包含总权值为负的回路**。
> ![800](image/9-图/最短路径-Floyd算法(多源最短).png)
> 在负权图中，Dijkstra 算法既不能保证每次选出的顶点都是真正的最近顶点，又不能保证已确定的最短路径不再被改变，因此 Dijsktra 算法不允许边的权为负。
> 每对顶点间的最短路径需要调用 Dijkstra 算法 n 次，时间复杂度 `O(n^3)`。

---

## 4.3. 拓扑排序(AOV网)

> **注意点**：拓扑排序的入度需要为 0，此时的顶点才可加入序列。
> ![800](image/9-图/图的应用-拓扑排序(AOV网)2.png)
> 首个顶点必定是 `a` 因为其入度为 0。下个顶点可以是 `e`、`b`。
> ① 选择顶点 `e`，此时下一个顶点只能是 `b`，因为顶点 `d` 的入度为 1，最终得到序列：`a e b c d`。
> ② 选择顶点 `b`，此时下一个顶点可以是 `e`、`c`。这里就分为两种情况：
>   1) `a b e c d`；2) `a b c e d`。


> **注意点**：拓扑排序的入度需要为 0，举例反驳即可。**碰到绝对的描述，需要考虑极端情况是否也成立，谨慎选择！**
> ![800](image/9-图/图的应用-拓扑排序(AOV网)3.png)
> 上图的例子只能证明其存在，是否唯一还未知。
> 因此如果矩阵是 `[0 1 1; 0 0 0; 0 0 0]`，此时的序列存在两种情况。
> 因此是存在，可能不唯一的。


> **注意点**：拓扑排序的入度需要为 0。也可以直接自行构造一个拓扑序列，若不存在两个同时入度为 0 的顶点，此时的拓扑序列是唯一的。
> ![800](image/9-图/图的应用-拓扑排序(AOV网)4.png)
> A/B：当拓扑序列为 `3 1` 时，顶点 `2` 的入度为 1，因此下一个顶点不可能是 `2`。
> C：当拓扑序列为 `3 1 4 2` 时，顶点 `5` 的入度为 1，因此下一个顶点不可能是 `5`。
> 这题的拓扑序列存在且唯一 `3 1 4 2 6 5`。


> **注意点**：拓扑排序的入度需要为 0。
> ![800](image/9-图/图的应用-拓扑排序(AOV网)5.png)
> D：当拓扑序列为 `5` 时，顶点 `2` 的入度为 1，因此下一个顶点不可能是 `2`。


> **注意点**：拓扑排序的入度为 0。若是不同时存在两个顶点的入度均为 0，那么此时的拓扑排序序列是唯一的。
> ![800](image/9-图/图的应用-拓扑排序(AOV网)6.png)

---

## 4.4. 关键路径(AOE网)

> **注意点**：关键路径指的是从源点到目标点之间的最长路径。其上经过的顶点称为关键结点，路径称为关键活动。
> ![800](image/9-图/图的应用-关键路径(AOE网).png)
> 此时的关键路径是 `V0 V1 V4 V6 V8`，最终的关键路径长度是 `a1 + a4 + a7 + a10 = 18`。


> **注意点**：
> **一个事件的最迟发生事件** 
> = min{以该事件为尾的弧的活动的最迟开始时间} 
> = min{以该事件为尾的弧所指事件的最迟发生事件与该弧的活动的持续时间之差}。
> 改变 AOE 网中的任何一个活动的持续时间，需要重新计算关键活动，可能导致关键路径改变。(*可能将关键活动->非关键活动；非关键活动->关键活动*)
> ![800](image/9-图/图的应用-关键路径(AOE网)2.png)


> **注意点**：关键路径和关键路径长度相关概念。
> ![800](image/9-图/图的应用-关键路径(AOE网)3.png)
> Ⅰ：改变关键路径上的任意关键活动，可能导致这个关键活动变为非关键活动从而产生不同的关键路径，也可能会缩短关键路径长度但不改变关键路径，还可能增大这个活动从而只增加关键路径长度，因此 **“必然”** 描述绝对。
> Ⅲ：缩短任意一个关键活动的持续时间，可能会导致出现新的关键路径，但是关键路径长度不变。
> Ⅴ：缩短多条关键路径，它们共有的一个关键活动持续时间缩短，不代表其他路径也会经过这个活动，若是缩短(*假设缩为 0*)，此时可能产生一些新的关键路径，此时的关键路径长度还可能不变。


> **注意点**：邻接矩阵的概念以及 AOE 网的概念。
> ![800](image/9-图/图的应用-关键路径(AOE网)4.png)
> 邻接矩阵列值表示该顶点的入度情况。
> 如果 i 列的值为 `∞` 那么说明这时候的入度不存在，即为 0。
> 那么 AOE 网和 AOV 类似，均从入度为 0 的顶点开始，此时如果关键路径存在，那么第 i 个顶点就作为源顶点，即起点。


> **注意点**：找出关键路径，以及关键活动，综合考虑缩短时间进而缩短总工期。
> ![800](image/9-图/图的应用-关键路径(AOE网)5.png)
> 此时的关键路径有三条：
> `b d e h`；`b d c g`；`b f h`
> 那么根据选项，所选的活动必须同时可以影响到三条关键路径。
> 因此 C 项：`f` 和 `d` 两点成立。其中活动 `d` 在第 1，2 条路径中存在，活动 `f` 仅在第 3 条路径中存在。


> **注意点**：最早开始时间选择最短路径长度；最迟开始时间，选择关键路径长度减去路径上的活动。
> ![800](image/9-图/图的应用-关键路径(AOE网)6.png)
> **最早开始时间**：c+b=12；**最迟开始时间**：关键路径长度{c+f+h}-活动结束时的顶点以及该活动的权值{g+d}=14


> **注意点**：关键路径和关键活动时间缩短的概念。
> ![800](image/9-图/图的应用-关键路径(AOE网)7.png)
> A：关键路径指的是从源点到汇点之间的路径长度最长的路径，边数仅代表活动较多，但并不影响判断关键路径。
> C/D：增加任意一个关键活动的时间，必定会增长工期，因为关键路径没有被改变；反过来如果缩短，则可能不会缩短工期，因为可能会产生新的关键路径，而新的关键路径长度不变。


> **注意点**：时间余量=最迟发生时间-最早发生时间-该活动持续时间。这里需要分别列出选项中的最早和最迟时间，总而得到时间余量。
> ![800](image/9-图/图的应用-关键路径(AOE网)8.png)
> 确定关键路径：`1 3 4 6`，其上关键活动：`b e i`，关键路径长度：`12`。
> 根据下表可得：
> c 时间余量 = vl(3)-ve(2)-1 = 5-2-1 = 2；
> g 的时间余量 = vl(6)-ve(3)-1 = 12-5-1 = 6；
> h 的时间余量 = vl(5)-ve(4)-1 = 11-8-1 = 2；
> j 的时间余量 = vl(6)-ve(5)-1 = 12-9-1 = 2。
> 综上，g 的时间余量最大。

|      **编号**      |  1  |  2  |  3  |  4  |  5  |  6  |
| :----------: | :-: | :-: | :-: | :-: | :-: | :-: |
| **最早开始时间 ve(i)** |  0  |  2  |  5  |  8  |  9  | 12  |
| **最迟开始时间 vl(i)** |  0  |  4  |  5  |  8  | 11  | 12  |

---

## 4.5. 有向无环图的应用

> **注意点**：有向无环图可以将重复的顶点缩减至只留关键顶点。
> ![800](image/9-图/图的应用-有向无环图的应用.png)
> 这里唯独需要注意的是表达式中的乘法 `*` 是省略没加进去的，因此构造图时需要注意。
> 括号不参与运算，只强调运算的先后顺序，因此无须将其加入图中。图的各个顶点只包含：**运算数**、**运算符**。


> **注意点**：图的相关概念，综合性概念。
> ![800](image/9-图/图的应用-有向无环图的应用2.png)
> A：如果这个有向图是一个环或者说只有两个顶点，但这两个顶点相互指向，是个强连通图，此时的入度至少为 1。
> B：无环图的拓扑序列是存在的，但是不一定唯一：如果存在两个入度同时为 0 的顶点，此时的拓扑序列就不唯一。
> C：如果每个顶点度都≥2，此时存在任意一点都可以到到达其他顶点，且不会停止(*存在任意路径*)。
> D：BFS 仅适用于无权图(*或所有边权值相等*)的单源最短路径。
